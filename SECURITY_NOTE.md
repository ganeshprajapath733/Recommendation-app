> **Security Notice:**
>
> This project contains a demo where an API key may be embedded directly in source code. Hard-coding secrets is insecure — do not commit real keys to public repositories. For production, use environment variables or a secrets manager (e.g., GitHub Actions secrets, AWS Secrets Manager, Azure Key Vault).

